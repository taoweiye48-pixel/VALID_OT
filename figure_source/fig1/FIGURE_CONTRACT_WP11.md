# Figure 1 WP11-linked variant: contract

## Core conclusion

VALID-OT fixes the audit tuple, embeds Arm R and Arm N in the same deleted-
versus-retained coefficient plane used by WP11, keeps Arm S outside that path
family as an objective-scale control, and separates two local-fidelity
diagnostics from external utility.

## Panel map

- **a:** unchanged audit instance and response quantities.
- **b:** Arm R and Arm N are paths on the \((u,v)\) coefficient plane. Arm R
  changes deletion coefficient \(u\) while fixing retained coefficient \(v\);
  Arm N changes both \(u\) and \(v\). Arm S is a subordinate synchronous-scale
  control and is not placed on the plane.
- **c:** unchanged local-response, endpoint-transport and end-to-end evidence
  hierarchy with external utility separated.

## Reviewer-risk controls

- The \((u,v)\) panel defines geometry only; it contains no WP11 response
  values or mechanism result.
- Arm S is not a third path and not an additional biological intervention.
- Arm R/N differences are not labelled as local-response fidelity failures.
- The same Arm R/N path colors should be reused when paths are overlaid on the
  later WP11 response surface.
