# Figure 1 coefficient-space variant: QA notes

- The panel-b title uses publication language and does not expose internal work
  package numbering.
- The \((u,v)\) plane contains path definitions only, not response-surface
  measurements or inferred mechanism effects.
- Arm R and Arm N are directly labelled and have distinct paths and colours.
- Arm S is visually subordinate and explicitly outside the \((u,v)\) path
  family.
- All explicit text sizes are at least 5 pt.
- Static source preflight: 14 passed, 0 warnings, 0 failures.
- Final width: 177.8 mm.
- Exports: editable SVG and PDF, 300 dpi PNG, and 600 dpi LZW TIFF.
- Visual QA was performed at full resolution and on a 720-pixel final-width
  preview.
