# Figure source

Each figure directory contains the reproducible plotting script, figure
contract/legend and the CSV/TSV/JSON source data used to create the manuscript
panel. Generated raster files and editor previews are intentionally omitted
from this directory; final PDF figures used by the manuscript are retained in
`paper/figures/`.
