# Protocol amendment 002: upper scale probe

**时间**：2026-07-16，外部数据下载期间，读取任何D1/D2科学结果之前。  
**修改**：规模稳健性上限从 `n=1600` 改为 `n=1500`。  

主真实pair预处理已冻结为每张最多1,500个单位。为避免在稳健性阶段使用不同的数据池并把下采样差异混入规模效应，上限与主预处理统一为1,500。其余规模仍为400和800；方法、指标和Gate不变。
